function showSuccessMessage(message) {
    alert(message); 
  }
  
  const formInfo = document.getElementById('updateInfo');
  const formPsw = document.getElementById('updatePassword');
  
  if (formInfo) {
    formInfo.addEventListener('submit', (event) => {
   
    });
  }
  
  if (formPsw) {
    formPsw.addEventListener('submit', (event) => {
  
    });
  }