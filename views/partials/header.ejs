<header id="top-header">
    <span class="siteName">
      <a href="/" title="Return to home page">CSE Motors</a>
    </span>

    <!--week05 account management activity : modify header -->
    <div id="tools">
      <% if (locals.accountData) { %>
        <a href="/account/account-management">Welcome <%= locals.accountData.account_firstname %></a>
        <a href="/account/logout">Logout</a>
      <% } else { %>
        <a href="/account/login">My Account</a>
      <% } %>
    </div>
  </header>